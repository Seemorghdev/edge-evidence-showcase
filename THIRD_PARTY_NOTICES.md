# Third-party notices

The showcase runtime itself uses only the Python standard library. Development tests
use pytest.

Related generated worker products may expose optional integrations with Google ADK and
Google Cloud Storage client libraries. Those projects are distributed under their own
terms, including Apache License 2.0 notices where applicable. Their names are used only
to describe optional interoperability and do not imply endorsement.

The exact dependency and license inventory must be rechecked from the frozen candidate
environment before any public write.
